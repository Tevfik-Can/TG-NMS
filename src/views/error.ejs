<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error | TG-NMS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6 text-center">
                <h1 class="display-1 text-danger mb-4"><%= error.status || 500 %></h1>
                <h2 class="mb-3"><%= message %></h2>
                <% if (process.env.NODE_ENV === 'development' && error.stack) { %>
                    <pre class="text-start bg-light p-3 mt-3"><%= error.stack %></pre>
                <% } %>
                <a href="/" class="btn btn-primary mt-4">Return Home</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 